# AI-DLC Flow

This directory contains compact specs.md AI-DLC configuration for this repository.

Primary runtime context lives in `memory-bank/`. The agent files here describe the intended roles and routing behavior. Full specs.md can be installed later if the project needs the complete generated command and skill set.
