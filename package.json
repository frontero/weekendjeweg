{
  "name": "weekendjeweg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "test:unit": "node --test tests/foundation.test.mjs",
    "test:e2e": "npm run build && playwright test"
  },
  "dependencies": {
    "nuxt": "latest",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "latest",
    "vue-tsc": "latest"
  }
}
